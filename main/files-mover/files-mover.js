const path = require('path');
const fs = require('fs');

exports.moveFiles = function (sheetDirectory, jobInfos, storageFolder) {

    return new Promise((resolve, reject) => {
        resolve();
    });
}